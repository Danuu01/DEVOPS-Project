apiVersion: v1
kind: Service
metadata:
  name: goal-tracker-service
  labels:
    app: goal-tracker
spec:
  selector:
    app: goal-tracker
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP  # Change to LoadBalancer or NodePort for external access
